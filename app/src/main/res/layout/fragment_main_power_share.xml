<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.inquirly.powershare.fragment.MainPowerShareFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:id="@+id/linear_mainMain">

        <android.support.design.widget.TabLayout
            android:id="@+id/powerTabLayout"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            android:background="@color/background_action_bar"
            app:layout_collapseMode="none"
            android:layout_marginTop="?android:attr/actionBarSize"
            app:tabIndicatorColor="@color/accent_material_dark"
            app:tabIndicatorHeight="4dp"
            app:tabSelectedTextColor="@android:color/white"
            app:tabTextColor="@color/white"
            app:tabGravity="fill"/>

        <android.support.v4.view.ViewPager
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/powerPager">
        </android.support.v4.view.ViewPager>

    </LinearLayout>

</LinearLayout>
